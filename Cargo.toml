[package]
name = "jord"
version = "0.18.0"
edition = "2024"
rust-version = "1.85"
authors = ["Cedric Liegeois <omam.github@gmail.com>"]
repository = "https://github.com/ofmooseandmen/jord-rs"
documentation = "https://docs.rs/jord"
keywords = ["geography", "geospatial", "n-vector", "ecef", "wgs84" ]
description = "Geographical Position Calculations (Ellipsoidal and Spherical models)"
license = "MIT"
readme = "README.md"
categories = ["science", "mathematics"]

[features]
default = []
serde = ["dep:serde"]
uom = ["dep:uom"]
geo-types = ["dep:geo-types"]
geo-traits = ["dep:geo-traits"]

[dependencies]
serde = { version = "1", optional = true, features = ["derive"] }
uom = { version = "0.38", optional = true }
geo-types = { version = "0.7.20", optional = true }
geo-traits = { version = "0.3.0", optional = true }

[dev-dependencies]
criterion = { version = "0.5.1", features = ["html_reports"] }

[[bench]]
name = "cap"
harness = false

[[bench]]
name = "chord_length"
harness = false

[[bench]]
name = "minor_arc"
harness = false

[[bench]]
name = "positions"
harness = false

[[bench]]
name = "rectangle"
harness = false

[[bench]]
name = "sphere"
harness = false

[[bench]]
name = "sloop"
harness = false

[[bench]]
name = "vec3"
harness = false
